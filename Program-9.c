/*Instruction: The below program is to read a one-dimensional array and print that array using functions.

Fill in the missing code so that it produces the desired output.*/


#include <stdio.h>
void read(int [], int);
void display(int [], int);
void main() {
	int a[10], n;
	printf("Enter n value : ");
	scanf("%d", &n);
	read( ); // Write the arguments in the function call
	
	display( ); // Write the arguments in the function call
}
void read( ) { // Write the arguments in the function definition
	printf("Enter %d elements : ", n);
	for ( ) { // Complete the code in for
		scanf("%d", ); // Complete the statement
	}
}
void display( ) { // Write the arguments in the function definition
	printf("Elements in the array are : ");
	for ( ) { // Complete the code in for
		printf("%d ", ); // Complete the statement
	}
}

